#include <stdio.h>

using namespace std;

int main()
{
    unsigned long long n;
    scanf("%ll",&n);
    unsigned long long arr[n]={0};
    char prime[n]={0};
    unsigned ll
    for(i=0;i<n;i++)
    {
        scanf("%ll",&arr[i]);
    }
}
